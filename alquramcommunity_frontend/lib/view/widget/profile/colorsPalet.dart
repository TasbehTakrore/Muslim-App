import 'package:flutter/Material.dart';

class ColorsPalet extends StatelessWidget {
  const ColorsPalet({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}